html
  head
    title #{title}
    meta(name='description', content='Profile')
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='style.css')
  body
    main
      span(id="inside")
      span(aria-label="people")  👯👬🏃👫
      br
      br
      p
        button(id="in") + 😀
        button(id="out") - 😀
      br
      p
        span(id="visits")
        span(id="footsteps" aria-label="footsteps") 👣
      p
        button(id="reset" aria-label="bin") 🗑
        
      br
    footer
      a(href='/logout' aria-label="door") 🚪
      span #{title} (#{username})
    script(src="profile.js")
