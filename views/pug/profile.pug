html
  head
    title #{title}
    meta(name='description', content='Profile')
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='style.css')
  body
    button(id="reset" aria-label="bin") 🗑
    h4 #{title} (#{username})
    fieldset
      span(id="inside")
      span(aria-label="people")  👯👬🏃👫
      p
        button(id="in") + 😀
        button(id="out") - 😀
      p
        span(id="visits")
        span(aria-label="footsteps")  👣
    p
      a(href='/logout' aria-label="door") 🚪
    script(src="profile.js")
